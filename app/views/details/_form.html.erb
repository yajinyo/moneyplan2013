<%= form_for @details do |f| %>

    <div id="open_groups">
      <%= f.hidden_field :group_id, :id => "selected_group_id" %>
      <!--input type="hidden" id="selected_group_id" name="group_id"-->
      分類：<div id="selected_group_name"></div>
      <a >
    </div>
    <div id="open_kinds">
      <%= f.hidden_field :kind_id, :id => "selected_kind_id" %>
      <!--input type="hidden" id="selected_kind_id" name="kind_id"-->
      種類：<div id="selected_kind_name"></div>
    </div>
    商品名：<%= f.text_field :article %>
    金額：<%= f.text_field :price %>

  </div>
  <div class="actions">
    <%= f.submit "登録", class => "btn btn-primary" %>
    <!--input type="submit" class="btn btn-primary" value="登録" /-->
  </div>

<%=end%>
